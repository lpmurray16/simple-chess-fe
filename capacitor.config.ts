import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'com.loganmurray.simplewizardschess',
    appName: 'SimpleWizardsChess',
    webDir: 'dist/simple-chess-fe/browser',
};

export default config;
